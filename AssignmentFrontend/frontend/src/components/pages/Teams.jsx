import React from 'react'
import TeamCards from '../TeamCards'

//similarly to the players webpage, the etams webpage generates all 6 teams in cards once selected and loaded.
function Teams() {
    return (
        <>
        <TeamCards/>
        </>
    )
}

export default Teams
